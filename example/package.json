{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build-lib && next dev",
    "build": "npm run build-lib && next build",
    "start": "next start",
    "lint": "next lint",
    "build-lib": "npm run build --prefix ../"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "next": "^14",
    "use-next-sse": "file://../"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
