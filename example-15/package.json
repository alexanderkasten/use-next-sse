{
  "name": "example-15",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build-lib && next dev",
    "build": "npm run build-lib && next build",
    "start": "next start",
    "lint": "next lint",
    "build-lib": "npm ci --prefix ../ && npm run build --prefix ../"
  },
  "dependencies": {
    "next": "^15.5.12",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "use-next-sse": "file://../"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
